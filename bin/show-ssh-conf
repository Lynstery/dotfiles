#!/usr/bin/env bash
host="$1"
if [ -z "$host" ]; then
  echo "Usage: sshconf <hostname>"
  exit 1
fi

awk -v host="$host" '
  $1 == "Host" && $2 == host {flag=1; print; next}
  $1 == "Host" && $2 != host {flag=0}
  flag
' ~/.ssh/config
